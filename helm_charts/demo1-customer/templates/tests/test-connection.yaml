apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo1-customer.fullname" . }}-test-connection"
  labels:
    {{- include "demo1-customer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo1-customer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
