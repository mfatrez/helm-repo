apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hass.fullname" . }}-test-connection"
  labels:
    {{- include "hass.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hass.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
